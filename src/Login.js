import React from "react"
// import config from "./config"

export default function Login() {
  return (
    <div className="ml-3">
      <a
        className="btn font-weight-bold text-dark"
        href={`https://ybej3.sse.codesandbox.io/auth/twitter`}
      >
        Sign In
      </a>
    </div>
  )
}

// href={`${config.serverUrl}/auth/twitter`}
